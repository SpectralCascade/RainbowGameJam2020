#ifndef CAMERA_H
#define CAMERA_H

#include <Ossium.h>

using namespace Ossium;
/*
struct CameraSchema : public Schema<CameraSchema, 10>
{
    DECLARE_BASE_SCHEMA(CameraSchema, 10);

    M(float, width) = 0;
    M(float, height) = 0;
    M(float, zoomScale) = 1.0f;

};

class Camera : public Component, public CameraSchema
{
public:
    CONSTRUCT_SCHEMA(Component, CameraSchema);
    DECLARE_COMPONENT(Component, Camera);

    void OnLoadFinish();

};*/
#endif // CAMERA_H
